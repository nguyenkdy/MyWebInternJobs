<%-
  include("../layout", {
    title,
    currentUser,
    body: `
      <section class="page">
        <h1>${title}</h1>
        <p class="muted">Use the navigation to continue.</p>
      </section>
    `,
  })
%>

